server:
  port: 8080
  servlet:
    context-path: /kicks-hub-service

spring:
  application:
    name: kicks-hub-service
  datasource:
    url: "jdbc:mysql://localhost:3307/kicks_hub"
    username: root
    password: 171104
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true